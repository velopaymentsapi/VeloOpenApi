type: object
properties:
    amount:
        type: decimal
    dateTime:
        type: string
        format: date-time
    status:
        type: string
        enum: [PENDING, FAILED, CREDIT, DEBIT]
    events:
        type: array
        items:
            $ref: 'FundingEvent.yaml'
