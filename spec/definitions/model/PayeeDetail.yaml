description: Payee Detail Information returned by API
type: object
properties:
    payeeId:
        type: string
        format: uuid
    payorRefs:
        type: array
        items:
           $ref: PayorRef.yaml
    displayName:
        type: string
    email:
        type: string
    onboardedStatus:
        type: string
    ofacStatus:
        type: string