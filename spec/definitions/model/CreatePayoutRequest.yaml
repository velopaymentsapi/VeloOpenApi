type: object
#required:
#    - payorId
#    - payments
#    - sourceCurrency
properties:
    payorId:
        type: string
        format: uuid
    payments:
        type: array
        items:
            $ref: Payment.yaml
    sourceCurrency:
        type: string
        enum: [USD]